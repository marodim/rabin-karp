#include "rabin-karp.h"

int main()
{
   // char str[4];
    
    //strcpy(str, "234");
    //int h = hash(strlen(str), 0, 23, str);
    //Rolling* r = NULL;
    procurar(3,5,23,100,"23456","456");
    //printf("%d\n", r->h);

    return 0;
}